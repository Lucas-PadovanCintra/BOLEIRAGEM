<div class="container py-4">
  <div class="row justify-content-center">
    <div class="col-lg-8">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h2 fw-bold text-dark">Editar Jogador</h1>
        <div class="btn-group">
          <%= link_to "Ver Jogador", @player, class: "btn btn-outline-primary" %>
          <%= link_to "Voltar ao Mercado", players_path, class: "btn btn-secondary" %>
        </div>
      </div>

      <div class="card shadow-sm">
        <div class="card-body">
          <%= form_with(model: @player, local: true, class: "needs-validation", novalidate: true) do |form| %>
            <% if @player.errors.any? %>
              <div class="alert alert-danger" role="alert">
                <h4 class="alert-heading">
                  <i class="fas fa-exclamation-triangle me-2"></i>
                  <%= pluralize(@player.errors.count, "erro impediu", "erros impediram") %> que este jogador fosse salvo:
                </h4>
                <ul class="mb-0">
                  <% @player.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
            <% end %>

            <div class="row g-3">
              <!-- Player Basic Info Section -->
              <div class="col-12">
                <h5 class="fw-bold text-dark mb-3 border-bottom pb-2">
                  <i class="fas fa-user me-2"></i>Informações Básicas
                </h5>
              </div>

              <!-- Player Name -->
              <div class="col-md-6">
                <%= form.label :name, "Nome do Jogador", class: "form-label fw-medium required" %>
                <%= form.text_field :name, 
                    class: "form-control #{'is-invalid' if @player.errors[:name].any?}",
                    placeholder: "Ex: Lionel Messi",
                    required: true,
                    maxlength: 100
                %>
                <% if @player.errors[:name].any? %>
                  <div class="invalid-feedback">
                    <%= @player.errors[:name].first %>
                  </div>
                <% else %>
                  <small class="form-text text-muted">Nome completo do jogador</small>
                <% end %>
              </div>

              <!-- API ID -->
              <div class="col-md-6">
                <%= form.label :api_id, "ID da API", class: "form-label fw-medium required" %>
                <%= form.number_field :api_id, 
                    class: "form-control #{'is-invalid' if @player.errors[:api_id].any?}",
                    placeholder: "Ex: 12345",
                    required: true,
                    min: 1,
                    max: 999999999
                %>
                <% if @player.errors[:api_id].any? %>
                  <div class="invalid-feedback">
                    <%= @player.errors[:api_id].first %>
                  </div>
                <% else %>
                  <small class="form-text text-muted">Identificador único da API de futebol</small>
                <% end %>
              </div>

              <!-- Performance Section -->
              <div class="col-12">
                <h5 class="fw-bold text-dark mb-3 border-bottom pb-2 mt-4">
                  <i class="fas fa-chart-line me-2"></i>Desempenho e Avaliação
                </h5>
              </div>

              <!-- Rating -->
              <div class="col-md-6">
                <%= form.label :rating, "Avaliação", class: "form-label fw-medium required" %>
                <div class="input-group">
                  <%= form.range_field :rating, 
                      class: "form-range",
                      min: 1,
                      max: 100,
                      step: 1,
                      oninput: "updateRatingDisplay(this.value)",
                      required: true
                  %>
                </div>
                <div class="d-flex justify-content-between align-items-center mt-2">
                  <small class="text-muted">1 (Muito Fraco)</small>
                  <div class="text-center">
                    <span class="badge bg-primary fs-6" id="rating-display"><%= @player.rating || 50 %></span>
                    <div id="rating-stars" class="mt-1">
                      <!-- Stars will be generated by JavaScript -->
                    </div>
                  </div>
                  <small class="text-muted">100 (Excepcional)</small>
                </div>
                <% if @player.errors[:rating].any? %>
                  <div class="text-danger small mt-1">
                    <%= @player.errors[:rating].first %>
                  </div>
                <% else %>
                  <small class="form-text text-muted">Avaliação geral do jogador (1-100)</small>
                <% end %>
              </div>

              <!-- Price -->
              <div class="col-md-6">
                <%= form.label :price, "Preço", class: "form-label fw-medium required" %>
                <div class="input-group">
                  <span class="input-group-text">💰</span>
                  <%= form.number_field :price, 
                      class: "form-control #{'is-invalid' if @player.errors[:price].any?}",
                      placeholder: "1000000",
                      required: true,
                      min: 1000,
                      max: 999999999,
                      step: 1000
                  %>
                </div>
                <% if @player.errors[:price].any? %>
                  <div class="invalid-feedback">
                    <%= @player.errors[:price].first %>
                  </div>
                <% else %>
                  <small class="form-text text-muted">Valor do jogador no mercado (mínimo: 1.000)</small>
                <% end %>
              </div>

              <!-- Market Status Section -->
              <div class="col-12">
                <h5 class="fw-bold text-dark mb-3 border-bottom pb-2 mt-4">
                  <i class="fas fa-store me-2"></i>Status no Mercado
                </h5>
              </div>

              <div class="col-12">
                <div class="form-check form-switch">
                  <%= form.check_box :is_on_market, 
                      class: "form-check-input #{'is-invalid' if @player.errors[:is_on_market].any?}",
                      id: "is_on_market_switch"
                  %>
                  <%= form.label :is_on_market, "Jogador disponível no mercado", class: "form-check-label fw-medium" %>
                </div>
                <% if @player.errors[:is_on_market].any? %>
                  <div class="text-danger small mt-1">
                    <%= @player.errors[:is_on_market].first %>
                  </div>
                <% else %>
                  <small class="form-text text-muted">
                    Se marcado, o jogador aparecerá como disponível para contratação. 
                    Se desmarcado, apenas times que já possuem contrato com ele poderão vê-lo.
                  </small>
                <% end %>
              </div>

              <!-- Current Contracts Info (Read-only) -->
              <div class="col-12 mt-3">
                <div class="card bg-light">
                  <div class="card-body">
                    <h6 class="card-title fw-bold">
                      <i class="fas fa-file-contract me-2"></i>Contratos Atuais
                    </h6>
                    <% active_contracts = @player.player_contracts.where(is_expired: false) %>
                    <% if active_contracts.any? %>
                      <div class="row g-2">
                        <% active_contracts.each do |contract| %>
                          <div class="col-md-6">
                            <div class="border rounded p-2">
                              <strong><%= contract.team.name %></strong><br>
                              <small class="text-muted">
                                <%= contract.matches_played %>/<%= contract.contract_length %> partidas
                                • <%= contract.created_at.strftime("%b %Y") %>
                              </small>
                            </div>
                          </div>
                        <% end %>
                      </div>
                    <% else %>
                      <span class="text-muted">Nenhum contrato ativo</span>
                    <% end %>
                  </div>
                </div>
              </div>

              <!-- Action Buttons -->
              <div class="col-12 mt-4">
                <div class="d-flex gap-2 justify-content-end flex-wrap">
                  <%= form.submit "Salvar Alterações", class: "btn btn-success" %>
                  <%= link_to "Cancelar", player_path(@player), class: "btn btn-outline-secondary" %>
                  <%= link_to "Excluir Jogador", @player, 
                      data: { turbo_method: :delete, turbo_confirm: "Tem certeza que deseja excluir este jogador? Esta ação não pode ser desfeita e todos os contratos serão perdidos." },
                      class: "btn btn-outline-danger" %>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      </div>

      <!-- Help Section -->
      <div class="card mt-4 border-info">
        <div class="card-body">
          <h6 class="card-title fw-bold text-info">
            <i class="fas fa-question-circle me-2"></i>Dicas de Preenchimento
          </h6>
          <div class="row g-3 small">
            <div class="col-md-6">
              <ul class="mb-0">
                <li><strong>API ID:</strong> Deve ser único no sistema</li>
                <li><strong>Avaliação:</strong> Valores maiores indicam melhor desempenho</li>
                <li><strong>Preço:</strong> Influencia na decisão de contratação dos times</li>
              </ul>
            </div>
            <div class="col-md-6">
              <ul class="mb-0">
                <li><strong>Mercado:</strong> Jogadores fora do mercado só aparecem para times contratantes</li>
                <li><strong>Contratos:</strong> Jogadores com contratos ativos não podem ser excluídos</li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  function updateRatingDisplay(value) {
    const display = document.getElementById('rating-display');
    const starsContainer = document.getElementById('rating-stars');
    
    display.textContent = value;
    
    // Update stars (1-5 stars based on rating)
    const starCount = Math.ceil(value / 20);
    let starsHtml = '';
    for (let i = 1; i <= 5; i++) {
      if (i <= starCount) {
        starsHtml += '<i class="fas fa-star text-warning small"></i>';
      } else {
        starsHtml += '<i class="far fa-star text-muted small"></i>';
      }
    }
    starsContainer.innerHTML = starsHtml;
  }

  // Initialize rating display
  document.addEventListener('DOMContentLoaded', function() {
    const ratingField = document.querySelector('input[type="range"]');
    if (ratingField) {
      updateRatingDisplay(ratingField.value);
    }
  });

  // Form validation
  (function() {
    'use strict';
    window.addEventListener('load', function() {
      var forms = document.getElementsByClassName('needs-validation');
      var validation = Array.prototype.filter.call(forms, function(form) {
        form.addEventListener('submit', function(event) {
          if (form.checkValidity() === false) {
            event.preventDefault();
            event.stopPropagation();
          }
          form.classList.add('was-validated');
        }, false);
      });
    }, false);
  })();
</script>