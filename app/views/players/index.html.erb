<div class="py-4">
  <div class="d-flex justify-content-between align-items-center mb-4 container">
    <h1 class="h2 fw-bold text-dark">Mercado de Jogadores</h1>
  </div>

  <div class="mb-4 container" data-controller="players-form">
    <button id="filter-button" class="btn btn-outline-primary"
    data-players-form-target="filterButton"
    data-action="click->players-form#toggleForm">Filtros</button>
    <%= render partial: 'form-filtros', locals: { q: @q } %>
  </div>

  <div class="card shadow-sm">
    <div class="table-responsive">
      <table class="table table-striped table-hover mb-0">
        <thead class="table-light">
          <tr>
            <th scope="col" class="fw-bold text-muted small text-uppercase">Imagem</th>
            <th scope="col" class="fw-bold text-muted small text-uppercase"><%= sort_link(@q, :name, "Nome") %></th>
            <th scope="col" class="fw-bold text-muted small text-uppercase"><%= sort_link(@q, :real_team_name, "Time Real") %></th>
            <th scope="col" class="fw-bold text-muted small text-uppercase"><%= sort_link(@q, :position, "Posição") %></th>
            <th scope="col" class="fw-bold text-muted small text-uppercase"><%= sort_link(@q, :goals, "Gols") %></th>
            <th scope="col" class="fw-bold text-muted small text-uppercase"><%= sort_link(@q, :assists, "Assistências") %></th>
            <th scope="col" class="fw-bold text-muted small text-uppercase"><%= sort_link(@q, :successful_dribbles, "Dribles Bem Sucedidos") %></th>
            <th scope="col" class="fw-bold text-muted small text-uppercase"><%= sort_link(@q, :interceptions, "Interceptações") %></th>
            <th scope="col" class="fw-bold text-muted small text-uppercase"><%= sort_link(@q, :yellow_cards, "Cartões Amarelos") %></th>
            <th scope="col" class="fw-bold text-muted small text-uppercase"><%= sort_link(@q, :red_cards, "Cartões Vermelhos") %></th>
            <th scope="col" class="fw-bold text-muted small text-uppercase"><%= sort_link(@q, :faults_committed, "Faltas Cometidas") %></th>
            <th scope="col" class="fw-bold text-muted small text-uppercase"><%= sort_link(@q, :loss_of_possession, "Perda de Posse") %></th>
            <th scope="col" class="fw-bold text-muted small text-uppercase"><%= sort_link(@q, :frequency_in_field, "Frequência em Campo") %></th>
            <th scope="col" class="fw-bold text-muted small text-uppercase"><%= sort_link(@q, :rating, "Rating") %></th>
            <th scope="col" class="fw-bold text-muted small text-uppercase"><%= sort_link(@q, :price, "Preço") %></th>
            <th scope="col" class="fw-bold text-muted small text-uppercase">Ações</th>
          </tr>
        </thead>
        <tbody>
          <% @players.each do |player| %>
            <tr>
              <td><img src="https://escudosfc.com.br/images/<%= I18n.transliterate(player.real_team_name) %>" alt="<%= player.real_team_name %>" width="50"></td>
              <td>
                <div class="fw-medium text-dark"><%= player.name %></div>
              </td>
              <td>
                <div class="fw-medium text-dark"><%= player.real_team_name %></div>
              </td>
              <td>
                <div class="fw-medium text-dark"><%= player.position %></div>
              </td>
              <td>
                <div class="fw-medium text-dark"><%= player.goals %></div>
              </td>
              <td>
                <div class="fw-medium text-dark"><%= player.assists %></div>
              </td>
              <td>
                <div class="fw-medium text-dark"><%= player.successful_dribbles %></div>
              </td>
              <td>
                <div class="fw-medium text-dark"><%= player.interceptions %></div>
              </td>
              <td>
                <div class="fw-medium text-dark"><%= player.yellow_cards %></div>
              </td>
              <td>
                <div class="fw-medium text-dark"><%= player.red_cards %></div>
              </td>
              <td>
                <div class="fw-medium text-dark"><%= player.faults_committed %></div>
              </td>
              <td>
                <div class="fw-medium text-dark"><%= player.loss_of_possession %></div>
              </td>
              <td>
                <div class="fw-medium text-dark"><%= player.frequency_in_field %>%</div>
              </td>
              <td>
                <div class="d-flex align-items-center">
                  <span class="fs-5 fw-bold text-primary me-2"><%= number_with_precision(player.rating, precision: 1) %></span>
                </div>
              </td>
              <td>
                <span class="fs-5 fw-bold text-success">💰<%= player.price %></span>
              </td>
              <td>
                <% cooldown = player.player_cooldowns.find_by(team: current_user.teams.first) %>
                <% if cooldown %>
                  <span class="badge bg-warning text-dark">Indisponível por: <%= cooldown.matches_remaining %> partidas</span>
                <% elsif player.is_on_market %>
                  <span class="badge bg-success">Disponível</span>
                  <% if current_user.wallet&.balance && current_user.wallet.balance >= player.price %>
                    <%= button_to 'Contratar', purchase_player_path(player), method: :post, class: "btn btn-sm btn-success",
                        data: { turbo_confirm: "Confirmar contratação de #{player.name} por $#{player.price}? Seu saldo atual é $#{current_user.wallet&.balance || 0}." } %>
                  <% else %>
                    <button class="btn btn-sm btn-secondary" disabled>Saldo Insuficiente</button>
                  <% end %>
                <% else %>
                  <span class="badge bg-danger">Contratado por:</span>
                  <%= player.player_contracts.where(is_expired: false).first&.team&.name || 'Time desconhecido' %>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>

  <%= render partial: 'pagination', locals: { players: @players } %>
</div>
